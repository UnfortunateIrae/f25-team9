## Datasource (Postgres). Can be overridden via environment variables.
## Set SPRING_DATASOURCE_URL, SPRING_DATASOURCE_USERNAME, SPRING_DATASOURCE_PASSWORD
## or set JDBC_DATABASE_URL / JDBC_DATABASE_USERNAME / JDBC_DATABASE_PASSWORD
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-bitter-tooth-adhj6bdk-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:neondb_owner}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:npg_UmEDkaP29Mvq}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

spring.freemarker.template-loader-path=classpath:/templates
spring.freemarker.suffix=.ftlh

# Serve static frontend files from classpath:/static (default). Place your
# `index.html`, `profile.html`, `app.js` and `CSS/` under
# `src/main/resources/static/` to have them available at http://localhost:8080/
#
# Example: to temporarily override the DB for a run, in PowerShell:
# $env:SPRING_DATASOURCE_URL='jdbc:postgresql://...'
# $env:SPRING_DATASOURCE_USERNAME='user'
# $env:SPRING_DATASOURCE_PASSWORD='pass'
# cd backend-api; mvn spring-boot:run
